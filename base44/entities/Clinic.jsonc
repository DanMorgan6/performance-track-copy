{
  "name": "Clinic",
  "properties": {
    "address": {
      "type": "string"
    },
    "booking_url": {
      "description": "Default clinic-wide booking/appointment link",
      "type": "string"
    },
    "brand_color_primary": {
      "default": "#9333ea",
      "description": "Primary brand color (hex)",
      "type": "string"
    },
    "brand_color_secondary": {
      "default": "#06b6d4",
      "description": "Secondary brand color (hex)",
      "type": "string"
    },
    "clinic_id": {
      "description": "Tenant identifier; equal to the Clinic record id",
      "type": "string"
    },
    "clinician_seat_count": {
      "default": 0,
      "description": "Number of active clinicians (seats)",
      "type": "number"
    },
    "contact_email": {
      "description": "Main contact email",
      "type": "string"
    },
    "current_period_end": {
      "description": "Current billing period end date",
      "format": "date-time",
      "type": "string"
    },
    "current_period_start": {
      "description": "Current billing period start",
      "format": "date-time",
      "type": "string"
    },
    "logo_url": {
      "description": "Clinic logo image URL",
      "type": "string"
    },
    "monthly_price_pence": {
      "default": 0,
      "description": "Monthly subscription price in pence: 3000 for 0-5 practitioners, 4500 for 6-10 practitioners",
      "type": "number"
    },
    "name": {
      "description": "Clinic name",
      "type": "string"
    },
    "owner_email": {
      "description": "Clinic owner/admin email",
      "type": "string"
    },
    "phone": {
      "type": "string"
    },
    "stripe_customer_id": {
      "description": "Stripe customer ID",
      "type": "string"
    },
    "stripe_subscription_id": {
      "description": "Stripe subscription ID",
      "type": "string"
    },
    "subscription_status": {
      "default": "trialing",
      "description": "Subscription status from Stripe",
      "enum": [
        "trial",
        "trialing",
        "active",
        "past_due",
        "unpaid",
        "canceled",
        "incomplete"
      ],
      "type": "string"
    },
    "trial_end_date": {
      "description": "Trial end date (14 days after start)",
      "format": "date-time",
      "type": "string"
    },
    "trial_start_date": {
      "description": "Trial start date (14 days from this)",
      "format": "date-time",
      "type": "string"
    }
  },
  "required": [
    "name",
    "contact_email",
    "owner_email"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "clinic_admin"
          }
        }
      ]
    },
    "delete": {
      "$and": [
        {
          "$or": [
            {
              "user_condition": {
                "role": "admin"
              }
            },
            {
              "user_condition": {
                "role": "clinic_admin"
              }
            }
          ]
        },
        {
          "data.clinic_id": "{{user.data.clinic_id}}"
        }
      ]
    },
    "read": {
      "$or": [
        {
          "data.clinic_id": "{{user.data.clinic_id}}"
        },
        {
          "data.owner_email": "{{user.email}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "$and": [
            {
              "$or": [
                {
                  "user_condition": {
                    "role": "admin"
                  }
                },
                {
                  "user_condition": {
                    "role": "clinic_admin"
                  }
                }
              ]
            },
            {
              "data.clinic_id": "{{user.data.clinic_id}}"
            }
          ]
        },
        {
          "data.owner_email": "{{user.email}}"
        }
      ]
    }
  },
  "type": "object"
}